// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: agentanycast/v1/streaming.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";

export const protobufPackage = "agentanycast.v1";

export enum StreamEndReason {
  STREAM_END_REASON_UNSPECIFIED = 0,
  STREAM_END_REASON_COMPLETE = 1,
  STREAM_END_REASON_CANCELED = 2,
  STREAM_END_REASON_ERROR = 3,
  UNRECOGNIZED = -1,
}

export function streamEndReasonFromJSON(object: any): StreamEndReason {
  switch (object) {
    case 0:
    case "STREAM_END_REASON_UNSPECIFIED":
      return StreamEndReason.STREAM_END_REASON_UNSPECIFIED;
    case 1:
    case "STREAM_END_REASON_COMPLETE":
      return StreamEndReason.STREAM_END_REASON_COMPLETE;
    case 2:
    case "STREAM_END_REASON_CANCELED":
      return StreamEndReason.STREAM_END_REASON_CANCELED;
    case 3:
    case "STREAM_END_REASON_ERROR":
      return StreamEndReason.STREAM_END_REASON_ERROR;
    case -1:
    case "UNRECOGNIZED":
    default:
      return StreamEndReason.UNRECOGNIZED;
  }
}

export function streamEndReasonToJSON(object: StreamEndReason): string {
  switch (object) {
    case StreamEndReason.STREAM_END_REASON_UNSPECIFIED:
      return "STREAM_END_REASON_UNSPECIFIED";
    case StreamEndReason.STREAM_END_REASON_COMPLETE:
      return "STREAM_END_REASON_COMPLETE";
    case StreamEndReason.STREAM_END_REASON_CANCELED:
      return "STREAM_END_REASON_CANCELED";
    case StreamEndReason.STREAM_END_REASON_ERROR:
      return "STREAM_END_REASON_ERROR";
    case StreamEndReason.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** StreamStart initiates a streaming artifact transfer. */
export interface StreamStart {
  taskId: string;
  artifactId: string;
  artifactName: string;
  /** MIME type of the artifact */
  mediaType: string;
  /** 0 = unknown (open-ended stream) */
  totalChunks: number;
  metadata: { [key: string]: string };
}

export interface StreamStart_MetadataEntry {
  key: string;
  value: string;
}

/** StreamChunk carries a single chunk of artifact data. */
export interface StreamChunk {
  taskId: string;
  artifactId: string;
  /** 0-indexed chunk sequence number */
  sequence: number;
  /** chunk payload */
  data: Buffer;
  /** true if this is the final chunk */
  isLast: boolean;
}

/** StreamEnd signals completion (or cancellation) of a stream. */
export interface StreamEnd {
  taskId: string;
  artifactId: string;
  reason: StreamEndReason;
}

function createBaseStreamStart(): StreamStart {
  return { taskId: "", artifactId: "", artifactName: "", mediaType: "", totalChunks: 0, metadata: {} };
}

export const StreamStart: MessageFns<StreamStart> = {
  encode(message: StreamStart, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.artifactId !== "") {
      writer.uint32(18).string(message.artifactId);
    }
    if (message.artifactName !== "") {
      writer.uint32(26).string(message.artifactName);
    }
    if (message.mediaType !== "") {
      writer.uint32(34).string(message.mediaType);
    }
    if (message.totalChunks !== 0) {
      writer.uint32(40).int32(message.totalChunks);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      StreamStart_MetadataEntry.encode({ key: key as any, value }, writer.uint32(50).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamStart {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamStart();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.artifactId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          message.artifactName = reader.string();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.mediaType = reader.string();
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.totalChunks = reader.int32();
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          const entry6 = StreamStart_MetadataEntry.decode(reader, reader.uint32());
          if (entry6.value !== undefined) {
            message.metadata[entry6.key] = entry6.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamStart {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      artifactId: isSet(object.artifactId)
        ? globalThis.String(object.artifactId)
        : isSet(object.artifact_id)
        ? globalThis.String(object.artifact_id)
        : "",
      artifactName: isSet(object.artifactName)
        ? globalThis.String(object.artifactName)
        : isSet(object.artifact_name)
        ? globalThis.String(object.artifact_name)
        : "",
      mediaType: isSet(object.mediaType)
        ? globalThis.String(object.mediaType)
        : isSet(object.media_type)
        ? globalThis.String(object.media_type)
        : "",
      totalChunks: isSet(object.totalChunks)
        ? globalThis.Number(object.totalChunks)
        : isSet(object.total_chunks)
        ? globalThis.Number(object.total_chunks)
        : 0,
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: StreamStart): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.artifactId !== "") {
      obj.artifactId = message.artifactId;
    }
    if (message.artifactName !== "") {
      obj.artifactName = message.artifactName;
    }
    if (message.mediaType !== "") {
      obj.mediaType = message.mediaType;
    }
    if (message.totalChunks !== 0) {
      obj.totalChunks = Math.round(message.totalChunks);
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamStart>, I>>(base?: I): StreamStart {
    return StreamStart.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamStart>, I>>(object: I): StreamStart {
    const message = createBaseStreamStart();
    message.taskId = object.taskId ?? "";
    message.artifactId = object.artifactId ?? "";
    message.artifactName = object.artifactName ?? "";
    message.mediaType = object.mediaType ?? "";
    message.totalChunks = object.totalChunks ?? 0;
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseStreamStart_MetadataEntry(): StreamStart_MetadataEntry {
  return { key: "", value: "" };
}

export const StreamStart_MetadataEntry: MessageFns<StreamStart_MetadataEntry> = {
  encode(message: StreamStart_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamStart_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamStart_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamStart_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: StreamStart_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamStart_MetadataEntry>, I>>(base?: I): StreamStart_MetadataEntry {
    return StreamStart_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamStart_MetadataEntry>, I>>(object: I): StreamStart_MetadataEntry {
    const message = createBaseStreamStart_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

function createBaseStreamChunk(): StreamChunk {
  return { taskId: "", artifactId: "", sequence: 0, data: Buffer.alloc(0), isLast: false };
}

export const StreamChunk: MessageFns<StreamChunk> = {
  encode(message: StreamChunk, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.artifactId !== "") {
      writer.uint32(18).string(message.artifactId);
    }
    if (message.sequence !== 0) {
      writer.uint32(24).int32(message.sequence);
    }
    if (message.data.length !== 0) {
      writer.uint32(34).bytes(message.data);
    }
    if (message.isLast !== false) {
      writer.uint32(40).bool(message.isLast);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamChunk {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamChunk();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.artifactId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.sequence = reader.int32();
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.data = Buffer.from(reader.bytes());
          continue;
        }
        case 5: {
          if (tag !== 40) {
            break;
          }

          message.isLast = reader.bool();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamChunk {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      artifactId: isSet(object.artifactId)
        ? globalThis.String(object.artifactId)
        : isSet(object.artifact_id)
        ? globalThis.String(object.artifact_id)
        : "",
      sequence: isSet(object.sequence) ? globalThis.Number(object.sequence) : 0,
      data: isSet(object.data) ? Buffer.from(bytesFromBase64(object.data)) : Buffer.alloc(0),
      isLast: isSet(object.isLast)
        ? globalThis.Boolean(object.isLast)
        : isSet(object.is_last)
        ? globalThis.Boolean(object.is_last)
        : false,
    };
  },

  toJSON(message: StreamChunk): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.artifactId !== "") {
      obj.artifactId = message.artifactId;
    }
    if (message.sequence !== 0) {
      obj.sequence = Math.round(message.sequence);
    }
    if (message.data.length !== 0) {
      obj.data = base64FromBytes(message.data);
    }
    if (message.isLast !== false) {
      obj.isLast = message.isLast;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamChunk>, I>>(base?: I): StreamChunk {
    return StreamChunk.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamChunk>, I>>(object: I): StreamChunk {
    const message = createBaseStreamChunk();
    message.taskId = object.taskId ?? "";
    message.artifactId = object.artifactId ?? "";
    message.sequence = object.sequence ?? 0;
    message.data = object.data ?? Buffer.alloc(0);
    message.isLast = object.isLast ?? false;
    return message;
  },
};

function createBaseStreamEnd(): StreamEnd {
  return { taskId: "", artifactId: "", reason: 0 };
}

export const StreamEnd: MessageFns<StreamEnd> = {
  encode(message: StreamEnd, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.taskId !== "") {
      writer.uint32(10).string(message.taskId);
    }
    if (message.artifactId !== "") {
      writer.uint32(18).string(message.artifactId);
    }
    if (message.reason !== 0) {
      writer.uint32(24).int32(message.reason);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): StreamEnd {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseStreamEnd();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.taskId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.artifactId = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.reason = reader.int32() as any;
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): StreamEnd {
    return {
      taskId: isSet(object.taskId)
        ? globalThis.String(object.taskId)
        : isSet(object.task_id)
        ? globalThis.String(object.task_id)
        : "",
      artifactId: isSet(object.artifactId)
        ? globalThis.String(object.artifactId)
        : isSet(object.artifact_id)
        ? globalThis.String(object.artifact_id)
        : "",
      reason: isSet(object.reason) ? streamEndReasonFromJSON(object.reason) : 0,
    };
  },

  toJSON(message: StreamEnd): unknown {
    const obj: any = {};
    if (message.taskId !== "") {
      obj.taskId = message.taskId;
    }
    if (message.artifactId !== "") {
      obj.artifactId = message.artifactId;
    }
    if (message.reason !== 0) {
      obj.reason = streamEndReasonToJSON(message.reason);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<StreamEnd>, I>>(base?: I): StreamEnd {
    return StreamEnd.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<StreamEnd>, I>>(object: I): StreamEnd {
    const message = createBaseStreamEnd();
    message.taskId = object.taskId ?? "";
    message.artifactId = object.artifactId ?? "";
    message.reason = object.reason ?? 0;
    return message;
  },
};

function bytesFromBase64(b64: string): Uint8Array {
  return Uint8Array.from(globalThis.Buffer.from(b64, "base64"));
}

function base64FromBytes(arr: Uint8Array): string {
  return globalThis.Buffer.from(arr).toString("base64");
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}

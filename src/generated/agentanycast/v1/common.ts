// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.11.5
//   protoc               unknown
// source: agentanycast/v1/common.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Timestamp } from "../../google/protobuf/timestamp";

export const protobufPackage = "agentanycast.v1";

/** ConnectionType indicates how a peer is connected. */
export enum ConnectionType {
  CONNECTION_TYPE_UNSPECIFIED = 0,
  CONNECTION_TYPE_DIRECT = 1,
  CONNECTION_TYPE_RELAY = 2,
  UNRECOGNIZED = -1,
}

export function connectionTypeFromJSON(object: any): ConnectionType {
  switch (object) {
    case 0:
    case "CONNECTION_TYPE_UNSPECIFIED":
      return ConnectionType.CONNECTION_TYPE_UNSPECIFIED;
    case 1:
    case "CONNECTION_TYPE_DIRECT":
      return ConnectionType.CONNECTION_TYPE_DIRECT;
    case 2:
    case "CONNECTION_TYPE_RELAY":
      return ConnectionType.CONNECTION_TYPE_RELAY;
    case -1:
    case "UNRECOGNIZED":
    default:
      return ConnectionType.UNRECOGNIZED;
  }
}

export function connectionTypeToJSON(object: ConnectionType): string {
  switch (object) {
    case ConnectionType.CONNECTION_TYPE_UNSPECIFIED:
      return "CONNECTION_TYPE_UNSPECIFIED";
    case ConnectionType.CONNECTION_TYPE_DIRECT:
      return "CONNECTION_TYPE_DIRECT";
    case ConnectionType.CONNECTION_TYPE_RELAY:
      return "CONNECTION_TYPE_RELAY";
    case ConnectionType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** NATType indicates the detected NAT configuration. */
export enum NATType {
  NAT_TYPE_UNSPECIFIED = 0,
  NAT_TYPE_PUBLIC = 1,
  NAT_TYPE_PRIVATE = 2,
  UNRECOGNIZED = -1,
}

export function nATTypeFromJSON(object: any): NATType {
  switch (object) {
    case 0:
    case "NAT_TYPE_UNSPECIFIED":
      return NATType.NAT_TYPE_UNSPECIFIED;
    case 1:
    case "NAT_TYPE_PUBLIC":
      return NATType.NAT_TYPE_PUBLIC;
    case 2:
    case "NAT_TYPE_PRIVATE":
      return NATType.NAT_TYPE_PRIVATE;
    case -1:
    case "UNRECOGNIZED":
    default:
      return NATType.UNRECOGNIZED;
  }
}

export function nATTypeToJSON(object: NATType): string {
  switch (object) {
    case NATType.NAT_TYPE_UNSPECIFIED:
      return "NAT_TYPE_UNSPECIFIED";
    case NATType.NAT_TYPE_PUBLIC:
      return "NAT_TYPE_PUBLIC";
    case NATType.NAT_TYPE_PRIVATE:
      return "NAT_TYPE_PRIVATE";
    case NATType.UNRECOGNIZED:
    default:
      return "UNRECOGNIZED";
  }
}

/** PeerInfo describes a connected peer node. */
export interface PeerInfo {
  peerId: string;
  addresses: string[];
  connectionType: ConnectionType;
  connectedAt?: Date | undefined;
}

/** NodeInfo describes the local node's state. */
export interface NodeInfo {
  peerId: string;
  listenAddresses: string[];
  natType: NATType;
  connectedPeers: number;
  startedAt?: Date | undefined;
  version: string;
}

/** ErrorDetail provides structured error information. */
export interface ErrorDetail {
  code: string;
  message: string;
  metadata: { [key: string]: string };
}

export interface ErrorDetail_MetadataEntry {
  key: string;
  value: string;
}

function createBasePeerInfo(): PeerInfo {
  return { peerId: "", addresses: [], connectionType: 0, connectedAt: undefined };
}

export const PeerInfo: MessageFns<PeerInfo> = {
  encode(message: PeerInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.peerId !== "") {
      writer.uint32(10).string(message.peerId);
    }
    for (const v of message.addresses) {
      writer.uint32(18).string(v!);
    }
    if (message.connectionType !== 0) {
      writer.uint32(24).int32(message.connectionType);
    }
    if (message.connectedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.connectedAt), writer.uint32(34).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): PeerInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBasePeerInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.addresses.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.connectionType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 34) {
            break;
          }

          message.connectedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): PeerInfo {
    return {
      peerId: isSet(object.peerId)
        ? globalThis.String(object.peerId)
        : isSet(object.peer_id)
        ? globalThis.String(object.peer_id)
        : "",
      addresses: globalThis.Array.isArray(object?.addresses)
        ? object.addresses.map((e: any) => globalThis.String(e))
        : [],
      connectionType: isSet(object.connectionType)
        ? connectionTypeFromJSON(object.connectionType)
        : isSet(object.connection_type)
        ? connectionTypeFromJSON(object.connection_type)
        : 0,
      connectedAt: isSet(object.connectedAt)
        ? fromJsonTimestamp(object.connectedAt)
        : isSet(object.connected_at)
        ? fromJsonTimestamp(object.connected_at)
        : undefined,
    };
  },

  toJSON(message: PeerInfo): unknown {
    const obj: any = {};
    if (message.peerId !== "") {
      obj.peerId = message.peerId;
    }
    if (message.addresses?.length) {
      obj.addresses = message.addresses;
    }
    if (message.connectionType !== 0) {
      obj.connectionType = connectionTypeToJSON(message.connectionType);
    }
    if (message.connectedAt !== undefined) {
      obj.connectedAt = message.connectedAt.toISOString();
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<PeerInfo>, I>>(base?: I): PeerInfo {
    return PeerInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<PeerInfo>, I>>(object: I): PeerInfo {
    const message = createBasePeerInfo();
    message.peerId = object.peerId ?? "";
    message.addresses = object.addresses?.map((e) => e) || [];
    message.connectionType = object.connectionType ?? 0;
    message.connectedAt = object.connectedAt ?? undefined;
    return message;
  },
};

function createBaseNodeInfo(): NodeInfo {
  return { peerId: "", listenAddresses: [], natType: 0, connectedPeers: 0, startedAt: undefined, version: "" };
}

export const NodeInfo: MessageFns<NodeInfo> = {
  encode(message: NodeInfo, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.peerId !== "") {
      writer.uint32(10).string(message.peerId);
    }
    for (const v of message.listenAddresses) {
      writer.uint32(18).string(v!);
    }
    if (message.natType !== 0) {
      writer.uint32(24).int32(message.natType);
    }
    if (message.connectedPeers !== 0) {
      writer.uint32(32).int32(message.connectedPeers);
    }
    if (message.startedAt !== undefined) {
      Timestamp.encode(toTimestamp(message.startedAt), writer.uint32(42).fork()).join();
    }
    if (message.version !== "") {
      writer.uint32(50).string(message.version);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): NodeInfo {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseNodeInfo();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.peerId = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.listenAddresses.push(reader.string());
          continue;
        }
        case 3: {
          if (tag !== 24) {
            break;
          }

          message.natType = reader.int32() as any;
          continue;
        }
        case 4: {
          if (tag !== 32) {
            break;
          }

          message.connectedPeers = reader.int32();
          continue;
        }
        case 5: {
          if (tag !== 42) {
            break;
          }

          message.startedAt = fromTimestamp(Timestamp.decode(reader, reader.uint32()));
          continue;
        }
        case 6: {
          if (tag !== 50) {
            break;
          }

          message.version = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): NodeInfo {
    return {
      peerId: isSet(object.peerId)
        ? globalThis.String(object.peerId)
        : isSet(object.peer_id)
        ? globalThis.String(object.peer_id)
        : "",
      listenAddresses: globalThis.Array.isArray(object?.listenAddresses)
        ? object.listenAddresses.map((e: any) => globalThis.String(e))
        : globalThis.Array.isArray(object?.listen_addresses)
        ? object.listen_addresses.map((e: any) => globalThis.String(e))
        : [],
      natType: isSet(object.natType)
        ? nATTypeFromJSON(object.natType)
        : isSet(object.nat_type)
        ? nATTypeFromJSON(object.nat_type)
        : 0,
      connectedPeers: isSet(object.connectedPeers)
        ? globalThis.Number(object.connectedPeers)
        : isSet(object.connected_peers)
        ? globalThis.Number(object.connected_peers)
        : 0,
      startedAt: isSet(object.startedAt)
        ? fromJsonTimestamp(object.startedAt)
        : isSet(object.started_at)
        ? fromJsonTimestamp(object.started_at)
        : undefined,
      version: isSet(object.version) ? globalThis.String(object.version) : "",
    };
  },

  toJSON(message: NodeInfo): unknown {
    const obj: any = {};
    if (message.peerId !== "") {
      obj.peerId = message.peerId;
    }
    if (message.listenAddresses?.length) {
      obj.listenAddresses = message.listenAddresses;
    }
    if (message.natType !== 0) {
      obj.natType = nATTypeToJSON(message.natType);
    }
    if (message.connectedPeers !== 0) {
      obj.connectedPeers = Math.round(message.connectedPeers);
    }
    if (message.startedAt !== undefined) {
      obj.startedAt = message.startedAt.toISOString();
    }
    if (message.version !== "") {
      obj.version = message.version;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<NodeInfo>, I>>(base?: I): NodeInfo {
    return NodeInfo.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<NodeInfo>, I>>(object: I): NodeInfo {
    const message = createBaseNodeInfo();
    message.peerId = object.peerId ?? "";
    message.listenAddresses = object.listenAddresses?.map((e) => e) || [];
    message.natType = object.natType ?? 0;
    message.connectedPeers = object.connectedPeers ?? 0;
    message.startedAt = object.startedAt ?? undefined;
    message.version = object.version ?? "";
    return message;
  },
};

function createBaseErrorDetail(): ErrorDetail {
  return { code: "", message: "", metadata: {} };
}

export const ErrorDetail: MessageFns<ErrorDetail> = {
  encode(message: ErrorDetail, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.code !== "") {
      writer.uint32(10).string(message.code);
    }
    if (message.message !== "") {
      writer.uint32(18).string(message.message);
    }
    globalThis.Object.entries(message.metadata).forEach(([key, value]: [string, string]) => {
      ErrorDetail_MetadataEntry.encode({ key: key as any, value }, writer.uint32(26).fork()).join();
    });
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorDetail {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorDetail();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.code = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.message = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 26) {
            break;
          }

          const entry3 = ErrorDetail_MetadataEntry.decode(reader, reader.uint32());
          if (entry3.value !== undefined) {
            message.metadata[entry3.key] = entry3.value;
          }
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ErrorDetail {
    return {
      code: isSet(object.code) ? globalThis.String(object.code) : "",
      message: isSet(object.message) ? globalThis.String(object.message) : "",
      metadata: isObject(object.metadata)
        ? (globalThis.Object.entries(object.metadata) as [string, any][]).reduce(
          (acc: { [key: string]: string }, [key, value]: [string, any]) => {
            acc[key] = globalThis.String(value);
            return acc;
          },
          {},
        )
        : {},
    };
  },

  toJSON(message: ErrorDetail): unknown {
    const obj: any = {};
    if (message.code !== "") {
      obj.code = message.code;
    }
    if (message.message !== "") {
      obj.message = message.message;
    }
    if (message.metadata) {
      const entries = globalThis.Object.entries(message.metadata) as [string, string][];
      if (entries.length > 0) {
        obj.metadata = {};
        entries.forEach(([k, v]) => {
          obj.metadata[k] = v;
        });
      }
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorDetail>, I>>(base?: I): ErrorDetail {
    return ErrorDetail.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorDetail>, I>>(object: I): ErrorDetail {
    const message = createBaseErrorDetail();
    message.code = object.code ?? "";
    message.message = object.message ?? "";
    message.metadata = (globalThis.Object.entries(object.metadata ?? {}) as [string, string][]).reduce(
      (acc: { [key: string]: string }, [key, value]: [string, string]) => {
        if (value !== undefined) {
          acc[key] = globalThis.String(value);
        }
        return acc;
      },
      {},
    );
    return message;
  },
};

function createBaseErrorDetail_MetadataEntry(): ErrorDetail_MetadataEntry {
  return { key: "", value: "" };
}

export const ErrorDetail_MetadataEntry: MessageFns<ErrorDetail_MetadataEntry> = {
  encode(message: ErrorDetail_MetadataEntry, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.key !== "") {
      writer.uint32(10).string(message.key);
    }
    if (message.value !== "") {
      writer.uint32(18).string(message.value);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): ErrorDetail_MetadataEntry {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseErrorDetail_MetadataEntry();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.key = reader.string();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.value = reader.string();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): ErrorDetail_MetadataEntry {
    return {
      key: isSet(object.key) ? globalThis.String(object.key) : "",
      value: isSet(object.value) ? globalThis.String(object.value) : "",
    };
  },

  toJSON(message: ErrorDetail_MetadataEntry): unknown {
    const obj: any = {};
    if (message.key !== "") {
      obj.key = message.key;
    }
    if (message.value !== "") {
      obj.value = message.value;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<ErrorDetail_MetadataEntry>, I>>(base?: I): ErrorDetail_MetadataEntry {
    return ErrorDetail_MetadataEntry.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<ErrorDetail_MetadataEntry>, I>>(object: I): ErrorDetail_MetadataEntry {
    const message = createBaseErrorDetail_MetadataEntry();
    message.key = object.key ?? "";
    message.value = object.value ?? "";
    return message;
  },
};

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends { $case: string } ? { [K in keyof Omit<T, "$case">]?: DeepPartial<T[K]> } & { $case: T["$case"] }
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function toTimestamp(date: Date): Timestamp {
  const seconds = Math.trunc(date.getTime() / 1_000);
  const nanos = (date.getTime() % 1_000) * 1_000_000;
  return { seconds, nanos };
}

function fromTimestamp(t: Timestamp): Date {
  let millis = (t.seconds || 0) * 1_000;
  millis += (t.nanos || 0) / 1_000_000;
  return new globalThis.Date(millis);
}

function fromJsonTimestamp(o: any): Date {
  if (o instanceof globalThis.Date) {
    return o;
  } else if (typeof o === "string") {
    return new globalThis.Date(o);
  } else {
    return fromTimestamp(Timestamp.fromJSON(o));
  }
}

function isObject(value: any): boolean {
  return typeof value === "object" && value !== null;
}

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
